apiVersion: v1
kind: Service
metadata:
  name: {{ include "steer.fullname" . }}
  labels:
    {{- include "steer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "steer.selectorLabels" . | nindent 4 }}
